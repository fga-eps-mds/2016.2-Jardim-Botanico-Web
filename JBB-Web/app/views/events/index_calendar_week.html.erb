<body id="page-top">
  <br><br><br><br>
  <div class="col-md-6 col-md-offset-3">
    <h2>Calendário de Eventos</h2>
    <br>
    <div class="actions">
    <%= link_to 'Pesquisar por semana', show_event_calendar_url, class: "btn btn-primary"%>
    </div>
    <br>
    <%= week_calendar number_of_weeks: 1 do |date, events| %>
    <b><%= date.strftime("%d") %> </div></b>
    <% events.each do |event| %>
    <div>
     <%= event.jbb_space_requested %>
   </div>
   <% end %>
   <% end %>
   <div class= "actions" >
    <%= link_to 'Criar evento', events_url, class: "btn btn-primary"%>
    <a id="back_space" >
    <%= link_to 'Voltar', home_url, class: "btn btn-primary"%>
    </div>
  </div>
</body>
