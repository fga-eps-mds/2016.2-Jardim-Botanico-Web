<body id="page-top">
  <br><br><br><br>
  <div class="col-md-6 col-md-offset-3">
    <%= month_calendar visitations: @visitations do |date, visitations| %>
    <%= date %>
    <% visitations.each do |visitations| %>
    <div>
     <%= visitations.description %>
   </div>
   <% end %>
   <% end %>
   <div class= "actions" >
    <%= link_to 'Voltar', show_event_user_url, class: "btn btn-primary"%>
    </div>
  </div>
</body>
